import React from 'react';

const ChartClassPresence = (props) => {
  const { text } = props;
  return (
        <div>
            <p>{text}</p>
        </div>
  );
};

export default ChartClassPresence;
